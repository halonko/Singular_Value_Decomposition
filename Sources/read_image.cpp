// This is a personal academic project. Dear PVS-Studio, please check it.
// PVS-Studio Static Code Analyzer for C, C++, C#, and Java: http://www.viva64.com#include "read_image.h"

//
//Mat image;
//image = imread(argv[1], 1);
//Vec3b intensity = image.at<Vec3b>(y, x);
//uchar blue = intensity.val[0];
//uchar green = intensity.val[1];
//uchar red = intensity.val[2];
//int array[image.size().width][image.size().height];
//for(size_t i = 0; i < image.size().width; i++){
//for(size_t j = 0; j < image.size().height; j++){
//array[i][j] = image
//}
//}
//
//if (!image.data) {
//printf("No image data \n");
//return -1;
//}
//for (size_t i = 0; i < image.size; i++) {
//
//}
//imwrite("123.jpeg", image);
////    namedWindow("Display Image", WINDOW_AUTOSIZE );
////    imshow("Display Image", image);
//
//waitKey(0);